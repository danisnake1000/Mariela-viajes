import React from 'react'
import { useParams } from 'react-router'

export const DetallesCars = () => {

  const {id} = useParams()
     console.log(id);

  return (
    <div>
     
      
    </div>
  )
}
